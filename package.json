{
	"name": "node-write-to-json",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"start": "npm run sass:watch & echo \"Flip Mode is the greatest.\" && node ./bin/www",
		"sass": "node-sass -w -r public/stylesheets -o public/stylesheets",
		"sass:watch": "npm run sass && npm run sass --watch --recursive"
	},
	"dependencies": {
		"body-parser": "^1.19.1",
		"cookie-parser": "^1.4.6",
		"debug": "^4.3.3",
		"ejs": "^3.1.6",
		"express": "^4.17.2",
		"http-errors": "^2.0.0",
		"morgan": "^1.10.0",
		"node-sass": "^7.0.1"
	}
}
